apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-models-mongodb-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ecommerce-models-mongodb
  template:
    metadata:
      labels:
         app:  ecommerce-models-mongodb
    spec:
      containers:
        - name: ecommerce-models-mongodb
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: ecommerce-models-mongo-srv
spec:
  selector:
    app: ecommerce-models-mongodb
  ports:
    - name: ecommerce-models-mongodb
      protocol: TCP
      port: 27017
      targetPort: 27017 