apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-feedback-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-feedback
  template:
    metadata:
      labels:
        app: redis-feedback
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis-feedback-srv
spec:
  selector:
    app: redis-feedback
  ports:
    - name: redis
      port: 6379
      targetPort: 6379

