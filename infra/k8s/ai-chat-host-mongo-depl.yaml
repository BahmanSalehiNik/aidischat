apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-chat-host-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-chat-host-mongo
  template:
    metadata:
      labels:
        app: ai-chat-host-mongo
    spec:
      containers:
        - name: ai-chat-host-mongo
          image: mongo:7.0
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_DATABASE
              value: ai-chat-host
          volumeMounts:
            - name: ai-chat-host-mongo-storage
              mountPath: /data/db
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "200m"
      volumes:
        - name: ai-chat-host-mongo-storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: ai-chat-host-mongo-srv
spec:
  selector:
    app: ai-chat-host-mongo
  ports:
    - name: mongo
      protocol: TCP
      port: 27017
      targetPort: 27017

