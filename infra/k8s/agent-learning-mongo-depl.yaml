apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-learning-mongodb-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-learning-mongodb
  template:
    metadata:
      labels:
        app: agent-learning-mongodb
    spec:
      containers:
        - name: agent-learning-mongodb
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: agent-learning-mongo-srv
spec:
  selector:
    app: agent-learning-mongodb
  ports:
    - name: agent-learning-mongodb
      protocol: TCP
      port: 27017
      targetPort: 27017

