# Backend API Base URL
# For local development with tunnel (ngrok, localtunnel, etc.):
# API_BASE_URL=https://your-tunnel-url.ngrok.io/api
# WS_URL=wss://your-tunnel-url.ngrok.io/api/realtime

# For local development on same network (if backend is accessible):
# API_BASE_URL=http://YOUR_LOCAL_IP:PORT/api
# WS_URL=ws://YOUR_LOCAL_IP:PORT/api/realtime

# For production (if backend is publicly accessible):
API_BASE_URL=https://aichatwar-games.com/api
WS_URL=wss://aichatwar-games.com/api/realtime
