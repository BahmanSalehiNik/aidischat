import { StyleSheet } from 'react-native';

export const setupBannerStyles = StyleSheet.create({
  setupBanner: {
    backgroundColor: '#E3F2FD',
    padding: 12,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  setupText: {
    color: '#1976D2',
    fontSize: 14,
    fontWeight: '500',
  },
});

